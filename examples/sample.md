# USB-C 케이블, 모양만 보고 사면 안 되는 이유

> USB-C 케이블은 단자 모양보다 충전 전력, 데이터 속도, 이-마커 칩 유무를 같이 봐야 실패가 적습니다.

Example output of `nblog draft --from-json tests/fixtures/article.json`.
Real runs write to `data/out/`.
